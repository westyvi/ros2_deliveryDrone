{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            // ensure that ROS2.humble at the bottom of VSCode (in a banner) has a check, not an X. If not, do cmd+shift+p 
            // type in ROS, and select ROS run. At this point, running this configuration in the 'run and debug' tab in vscode should work!
            "name": "ROS: Debug hand tracker node",
            "type": "ros",
            "request": "launch",
            "target": "${workspaceFolder}/src/hand_detection/launch/hand_tracker_launch.py",
            "preLaunchTask": "make_debug",
        },
        {
            // ensure that ROS2.humble at the bottom of VSCode (in a banner) has a check, not an X. If not, do cmd+shift+p 
            // type in ROS, and select ROS run. At this point, running this configuration in the 'run and debug' tab in vscode should work!
            "name": "ROS: Debug video capture node",
            "type": "ros",
            "request": "launch",
            "target": "${workspaceFolder}/src/hand_detection/launch/video_capture_launch.py",
            "preLaunchTask": "make_debug",
        },
    ]
}
